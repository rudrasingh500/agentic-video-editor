# Backend API URL injected at build time
VITE_BACKEND_URL=http://localhost:8000

# Optional local dev auth defaults (do not ship secrets in VITE_ vars)
VITE_DEV_TOKEN=
VITE_SESSION_TOKEN=
VITE_WEBHOOK_TOKEN=
VITE_RENDER_WEBHOOK_SECRET=
